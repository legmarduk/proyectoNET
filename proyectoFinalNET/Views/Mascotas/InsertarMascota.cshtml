@using proyectoFinalNET.ViewModels
@model ListaTipos


@{
    Layout = null;
}

        <div>
            <select class="form-control">
                @foreach (TiposVista tv in Model.tipos)
                {
                    <option value="@tv.codigo">@tv.tipo_mascota</option>
                }
            </select>
        </div>